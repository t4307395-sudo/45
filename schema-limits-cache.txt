-- حد الفحوصات اليومي (3 لكل حساب/IP يومياً)
CREATE TABLE IF NOT EXISTS scan_limits (
    identifier TEXT NOT NULL,
    scan_date TEXT NOT NULL,
    count INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (identifier, scan_date)
);

-- كاش نتائج الفحص لمدة ساعة لنفس الرابط
CREATE TABLE IF NOT EXISTS analysis_cache (
    url TEXT PRIMARY KEY,
    result_json TEXT NOT NULL,
    cached_at INTEGER NOT NULL
);

-- سجل تاريخي بسيط لمقارنة "قبل وبعد"
CREATE TABLE IF NOT EXISTS scan_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    identifier TEXT NOT NULL,
    url TEXT NOT NULL,
    performance_score INTEGER,
    seo_score INTEGER,
    checked_at INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_scan_history_lookup ON scan_history (identifier, url, checked_at);
